#include <iostream>
#include <conio.h>
#include <math.h>
using namespace std;
int main()
{
	int x,f;
	cout<<"Enter x:";
	cin>> x;
	if (x<0)
	{
	    f=(5*pow(x,2)+3*x);
	}
	if((x<=5)&&(x>=0))
	{
		f=6*sqrt(x);
	}
	if(x>5)
	{
		f=(-x+107);
	}
cout<<"f(x):"<<f<<"\n";
return(0);	
}
